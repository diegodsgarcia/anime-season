# Animes Season Crawler API

Use yarn or npm to install all dependecies:

```ssh
yarn install

```

```ssh
npm install
```

You can run crawler with commmand:

```ssh
yarn start

```

```ssh
npm run start
```
